apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
 - eventlisteners
 - triggertemplates
 - pipelines
 - tasks
resources:
  - namespace.yml
  - operatorgroup.yml
  - subscription.yml